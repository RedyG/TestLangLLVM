/*#include "Modules.h"

std::unordered_map<std::string_view, RedyModule> modules;

void AddModule(std::string_view name, RedyModule&& module) {
	modules.emplace(name, std::move(module));
}

RedyModule& GetModule(std::string_view name) {
	return modules.at(name);
}*/